# Debe estar en la raiz de la db.sqlite3.

docker run -it --rm -v "$(pwd)":/data alpine:latest sh -c   "apk add --no-cache sqlite && sqlite3 /data/db.sqlite3"

# Creación de E/R a partir del esquema creado en código.

python manage.py graph_models register_par -o graph.dot
dot -Tpng graph.dot -o graph.png

# Creación de requirements.txt.

pip freeze > requirements.txt

# Uso de comando 'numequ'.

python manage.py numequ -h (para más información)

# Traer mis cambios de 'testing' a la rama 'main'.

git checkout main
git pull origin main
git merge testing
git push origin main
